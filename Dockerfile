# 🐳 1. استخدام صورة رسمية لـ Java
FROM openjdk:17-jdk-slim

# 📂 2. إنشاء مجلد داخل الحاوية لتخزين الكود
WORKDIR /app

# 📥 3. نسخ جميع ملفات المشروع إلى مجلد العمل داخل الحاوية
COPY . /app

# ⚙️ 4. تجميع ملفات Java إلى مجلد out
RUN javac -d out src/*.java

# 🚀 5. تحديد أمر التشغيل لتشغيل الكلاس الرئيسي
CMD ["java", "-cp", "out", "ChatServer"]

# 🌐 6. فتح البورت الذي يستخدمه الخادم (مثلاً 8080)
EXPOSE 8080


